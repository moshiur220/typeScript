/*
Type inference describes where and how TypeScript infers types when you don’t explicitly annotate them.
*/
// For example, consider the following TypeScript code:

let testNumber = 42;

const myObj = { foo: 42, bar: "hello" };
const myArray = [1, 2, 3];
